*.swp

# Local tt handoff artifacts
.tt/

# pi may write local auth/cache if a worker dir ever points here
pi-worker/auth.json

# skill eval scratch — transient runs, snapshots, logs
skills/*-workspace/

# pi runtime cache (auth, tasks, session state)
.pi/
